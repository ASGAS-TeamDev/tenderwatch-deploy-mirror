# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.log
backend/config/
backend/.env

# Node / Vite
node_modules/
dist/
.vite/
*.tsbuildinfo
coverage/
.env
.env.local
.env.production

# Vercel CLI metadata (project binding is set on first deploy, not committed)
.vercel/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code local settings (secrets: API keys, per-machine env)
.claude/settings.local.json

# Stitch / planning artefacts (kept in the planning repo, not here)
# (none — this is the build repo)
