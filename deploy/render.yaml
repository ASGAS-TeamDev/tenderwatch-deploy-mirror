# Render Blueprint for the Tender Watch backend.
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: tender-watch-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: "pip install -e ."
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: TW_API_BASE
        value: https://ocds-api.etenders.gov.za
      - key: TW_CONFIG_PATH
        value: /opt/render/project/src/backend/config/tender-watch.json
      - key: TW_CACHE_TTL_SECONDS
        value: "60"
      - key: TW_ALLOWED_ORIGINS
        value: https://frontend-ten-peach-39.vercel.app,https://frontend-lvruqog7e-herge1.vercel.app,http://localhost:5173
      - key: TW_LOG_LEVEL
        value: INFO
      - key: PYTHON_VERSION
        value: 3.12.0